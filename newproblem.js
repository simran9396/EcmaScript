const areaReactangle=(b,w)=>b*w
console.log (areaReactangle(10,27))

const areaTriangle=(b,h)=>(1/2)*b*h
console.log(`area of triangle is ${areaTriangle(12,27)} sq.unit`)

const areaCircle=(r)=>3.14*r*r

console.log(`Area of circle is ${areaCircle(5)}`)
const b=27
const r=7
const h=14
const r2=5
const x=12

console.log(`area of shaded portion is${areaReactangle(h,b-r)+(1/2*areaCircle
    
    
    
    
    
    
    
    (r))+areaTriangle(b-r,x)-areaCircle(r2)}`)
